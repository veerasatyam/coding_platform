class Solution {
    public int minFlips(String s) {
        String str = s + s;
        int n = s.length();
        int count1 = 0;
        int count2 = 0;
        for(int i=0;i<str.length();i++){
            char ch = str.charAt(i);
            
        }
    }
}